import { createSlice } from "@reduxjs/toolkit";

export const valuesSlice = createSlice({
    name: "startGameValues",
    initialState: {
        value: "",
    },
    reducers: {
        readInput: (state) => {
            
        }
    }
})